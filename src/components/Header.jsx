import React from 'react';
import '../styles/Header.css';

function Header() {
  return (
    <header className="hero-section">
      {/* Hero banner content here (like the background image & tagline) */}
    </header>
  );
}

export default Header;

